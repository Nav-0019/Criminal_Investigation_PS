[phases.setup]
# Install ffmpeg system package (required by Whisper for audio decoding)
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
